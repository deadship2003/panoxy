# Sources (src/) are separated from outputs; build artifacts never enter the repo.
bin/
dist/

# Offline-package staging outputs
panoxy-V*/
*.tar.gz
*.sha256

# Go test binaries
*.test

# Hand-edited config at the package root (contains real subscription URLs) — must never be committed
clash.yaml
